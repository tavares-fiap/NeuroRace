# ============================================================================
# PC1 - Central: Configuração para o ambiente principal
# ============================================================================

# Simulador A (Player 1)
ACQ_PORT_A=13854
PACKET_INTERVAL=1.0

# Acquisition A (Player 1)
PLAYER_ID_A=1
EEG_HOST_A=simulator-a
# EEG_HOST_A=host.docker.internal  # Use se conectar ao ThinkGear Connector local

# Simulador B (Player 2) - Comentado quando rodando em modo single-PC
# ACQ_PORT_B=13855
# PACKET_INTERVAL_B=1.0
# PLAYER_ID_B=2
# EEG_HOST_B=simulator-b

# Broker (Socket.IO)
BROKER_URL=http://broker:3000

# Threshold para qualidade do sinal NeuroSky
# 0 = sinal perfeito (uso real), 100+ = aceitar sinais ruins (simulação)
POOR_SIGNAL_LEVEL_THRESHOLD=0