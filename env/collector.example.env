# ============================================================================
# PC2 - Coletor: Configuração para o ambiente secundário
# ============================================================================

# Simulador B (Player 2)
ACQ_PORT_B=13854
PACKET_INTERVAL_B=1.0

# Acquisition B (Player 2)
PLAYER_ID_B=2
EEG_HOST_B=simulator-b
# EEG_HOST_B=127.0.0.1  # Use se o simulador estiver fora do Docker
# EEG_HOST_B=host.docker.internal  # Use se conectar ao ThinkGear Connector local

# Broker (Socket.IO) no PC1
# IMPORTANTE: Substitua pelo IP real do PC1 (ex: 192.168.15.10)
BROKER_URL=http://192.168.15.10:3000

# Threshold para qualidade do sinal NeuroSky
# 0 = sinal perfeito (uso real), 100+ = aceitar sinais ruins (simulação)
POOR_SIGNAL_LEVEL_THRESHOLD=0